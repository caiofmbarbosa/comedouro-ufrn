# Sistema de Alimentacao de Caes

Frontend em React + Vite integrado ao backend Spring Boot.

## Stack

- React + TypeScript
- Vite
- Axios para requisicoes HTTP
- Tailwind CSS para estilização por classes
- React Router DOM
- Lucide React para icones
- Webpack configurado como build alternativo

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm run build:webpack`

Configure `VITE_API_URL` se o backend nao estiver em `http://localhost:8080`.
